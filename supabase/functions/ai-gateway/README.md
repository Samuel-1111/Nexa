Deployed and ACTIVE on the live `nexa` Supabase project (function id 3f929dcf-847e-4d89-acde-2ddc92a8282b).
Source is identical to what's live -- copy in index.ts mirrors the deployed version.
Requires GEMINI_API_KEY project secret (see docs/SETUP.md) -- fails closed with 503 until set.
